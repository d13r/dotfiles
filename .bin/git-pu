#!/bin/sh

# git pu = Push Upstream (push current branch and set as upstream)
remote="${1:-origin}"
branch="${2:-}"

if [ -n "$branch" ]; then
    exec git push -u "$remote" "HEAD:$branch"
fi

exec git push -u "$remote" HEAD
