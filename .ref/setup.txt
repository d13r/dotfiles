Setting up a new account (any server/computer):

    Install Dotfiles
    setup-identity <email> <name>   - if not using my defaults (e.g. at work)

Setting up a new Ubuntu machine (requires root access):

    setup-sudo
    sudo timedatectl set-timezone Europe/London
    setup-hostname <fqdn>
    Create a swap file, if needed
    setup-utilities
    setup-brew
    setup-git

    setup-mariadb
    setup-apache
    setup-docker

On Ubuntu desktop:

    Move the laucher bar to the bottom and size to 16px
    remap-ctrl-alt-arrow-keys
    Configure other keys
    Install Tweaks - remap Caps Lock, disable suspend when lid closed
    Install Grub Customizer and tweak settings (if dual booting)
    Install Dropbox (enable selective sync if needed)
    Install KeePassXC
    Install Authy
    Configure Firefox
    Set a background image and colour scheme
    setup-phpstorm
    Install dbeaver-ce ?
    Install VLC ?

On a non-desktop with some GUI applications installed:

    disable-xdg-user-dirs

On AWS CloudShell:

    setup-aws-cloudshell
